name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'assets/ScienceMD-icon.png'
      - 'android/**'
      - '.github/workflows/build-android-apk.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'index.html'
      - 'assets/ScienceMD-icon.png'
      - 'android/**'
      - '.github/workflows/build-android-apk.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build APK
        working-directory: android
        shell: bash
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon :app:assembleDebug 2>&1 | tee build-output.log

      - name: Upload build log
        uses: actions/upload-artifact@v4
        with:
          name: ScienceMD-build-log
          path: android/build-output.log

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ScienceMD-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
